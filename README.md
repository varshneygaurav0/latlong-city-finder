# latlong-city-finder
A Python-based bulk reverse-geocoding tool that converts latitude/longitude coordinates into city, district, state, and country using OpenStreetMap’s Nominatim API. Optimized with caching, fail-safes, progress tracking, and CSV export for large datasets.
